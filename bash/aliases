# Alias definitions.
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# See /usr/share/doc/bash-doc/examples in the bash-doc package (Ubuntu).

if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi
